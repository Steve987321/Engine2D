#pragma once

#include <EngineCore.h>

#include <EngineMeta.h>

namespace Toad
{

	/*struct Sprite : Entity
	{
		sf::Sprite sprite;

		Sprite(uint32_t id, const sf::Sprite& sprite)
			: Entity(id),
			  sprite(sprite)
		{
		}

		Vec2f GetPosition() const
		{
			return sprite.getPosition();
		}

	};*/

}