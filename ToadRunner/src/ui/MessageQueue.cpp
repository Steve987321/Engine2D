#include "pch.h"
#include "engine/Engine.h"
#include "MessageQueue.h"

namespace Toad
{

	void MessageQueue::AddToMessageQueue(MessageQueueMessage msg_type)
	{
		//_messageQueue.emplace(std::move(msg_type), });
	}

	void MessageQueue::Show()
	{
	}
}